<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Producto</title>
</head>
<body>

    <main>
<div class="product-details">
    <div class="product-details-image">
        <img src="{{product.imageUrl}}" alt="{{product.name}}">
    </div>

    <div class="product-details-info">
        <h2>{{product.name}}</h2>
        <p>{{product.description}}</p>

        <div class="product-meta">
            <span><strong>Precio:</strong> ${{product.price}}</span>
            <span><strong>En stock:</strong> {{product.stock}}</span>
        </div>

<div class="cart-actions">
    <label for="quantity">Cantidad:</label>
    <input type="number" id="quantity" value="1" min="1" max="10">
    <button onclick="addToCart('{{product._id}}', '{{product.price}}', '{{product.imageUrl}}', document.getElementById('quantity').value)">Agregar al carrito</button>
</div>
    </div>
</div>
    </main>



 <!-- Asegúrate de tener la lógica del carrito en este archivo -->
</body>
</html>
